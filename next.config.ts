import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['img.clerk.com',"assets.aceternity.com"], // Add the hostname here
  }
  /* config options here */
};

export default nextConfig;